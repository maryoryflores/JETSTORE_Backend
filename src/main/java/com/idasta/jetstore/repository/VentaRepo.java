package com.idasta.jetstore.repository;

import com.idasta.jetstore.model.Venta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VentaRepo extends JpaRepository<Venta, Long> {
}
